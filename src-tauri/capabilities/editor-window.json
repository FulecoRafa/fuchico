{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "editor-window",
  "description": "Capability for single-file editor windows (issue #29)",
  "windows": ["editor-*"],
  "permissions": [
    "core:default",
    "core:webview:allow-create-webview-window",
    "opener:default",
    "dialog:allow-open"
  ]
}
